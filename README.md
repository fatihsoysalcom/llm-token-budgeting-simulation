# llm-token-budgeting-simulation
This example demonstrates the concept of token budgeting for Large Language Models (LLMs). It uses the `tiktoken` library to accurately count tokens in different text inputs, simulates a hypothetical token budget, and shows how to check if a prompt fits within the budget. It also illustrates a basic truncation strategy for prompts that exceed the a
